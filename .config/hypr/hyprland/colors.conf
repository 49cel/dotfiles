general {
    col.active_border = rgba(ab9e8977)
    col.inactive_border = rgba(7e735f55)
}

misc {
    background_color = rgba(17130bFF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Google Sans Flex Medium, Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(17130bFF)
        col.text = rgba(ece1d3FF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(ece1d3), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(ece1d3), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(ece1d3), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrule = border_color rgba(f9c03dAA) rgba(f9c03d77), match:pin 1
